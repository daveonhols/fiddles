{
  "name": "red-star-pass",
  "version": "1.0.0",
  "description": "Proof of concept password input control.  Allows users to pre-prend non printable ASCII chars to their passwords to defeat dictionary attacks and increase entropy.",
  "scripts": {
    "lint": "./node_modules/.bin/eslint js/*.js & exit 0",
    "build": "./node_modules/.bin/babel --presets es2015 --out-dir ./static/ ./js/*.js"
  },
  "author": "David Smith",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "eslint": "^4.3.0"
  },
  "devDependencies": {
    "eslint-config-airbnb-base": "^11.3.1",
    "eslint-plugin-import": "^2.7.0"
  }
}
